<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Song</title>
  </head>
  <body>
    <form action="/songs/<%= @song.slug %>" method="post">
      <p>Song Name: <input type="text" name="song[name]" value="<%= @song.name%>"></p>
      <p>Artist: <input type="text" name="artist[name]" value="<%= @song.artist.name %>"></p>

      <% Genre.all.each do |genre| %>
        <input type="checkbox" id="<%= genre.name %>" name="genres[]" value="<%= genre.id %>"><%= genre.name %>

      <% end %>

      <br><br>
      <input type="submit" value="Edit Song">
    </form>
  </body>
</html>
